class AppRadii {
  AppRadii._();

  static const double small = 16.0;  // .pen uses 16 for inputs and buttons
  static const double medium = 16.0;
  static const double large = 24.0;
  static const double pill = 500.0;
}
